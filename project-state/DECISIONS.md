# DECISIONS — INFLUENTZ

> Só decisões de consequência irreversível ou estrutural, com a evidência.
> Decisões de produto/dinheiro/legal vivem na SPEC — aqui só o que muda como o
> projeto é conduzido.

| Decisão | Evidência | Data |
|---|---|---|
| `main` é a única branch de trabalho real; sem fluxo de PR (dono único, sem revisor) | `git log`: 0 PRs em 59 commits | 11/09 |
| Fast-forward de `main` para o branch de 54 commits — zero conflito, `main` era ancestral puro | `git merge-base --is-ancestor` = true | 11/09 |
| Guarda de documentos estendido para cobrir telas (`.html`/`.dc.html`/`canvas.json`), não só `.md` | Achado da auditoria: violação de §4.9.1 publicada, guarda não via | 11/09 |
| "Cartão de crédito" é exceção nomeada na regra de palavra proibida — é o nome do método de pagamento, não o conceito de saldo/carteira que a SPEC §4.9.1 proíbe | Teste de regressão do guarda; SPEC usa a mesma expressão em prosa | 11/09 |
| Arquivo `influentz-wireframes-v1.html` (payload do skill de design) é gitignored e nunca é fonte — sempre regenerado dos `.dc.html` | `.gitignore` da própria pasta, comentado | 11/09 |
| Critério de aceite passa a ser obrigatório antes de qualquer código, começando pela fatia 1 (não as 91 funções de uma vez) | Instrução da recuperação, Passo 7 | 11/09 |
| `project-state/` criado como memória entre sessões — curto, não duplica SPEC/HISTORICO | Falha observada: continuidade dependia da conversa | 11/09 |
| Guarda de números ganhou exceção de subconjunto — "68 funções da Fatia 1" não é o total do v1; sem ela, o próprio critério de aceite acusaria falso positivo | Achado ao escrever ACEITE-FATIA-1.md; guarda disparou, corrigido e reprovado antes de seguir | 11/09 |
| Guarda de números reescrito para exigir contexto POSITIVO ("funções no v1"/"do lançamento"/"em três fatias"), não mais tentar excluir contexto negativo um a um — a exclusão por subconjunto ("da Fatia N") não pegava "44 funções" solto num resumo de categorias | Segundo falso positivo em QA-FATIA-1.md; corrigido, testado com regressão nos 17 documentos + 3 casos positivos sintéticos | 11/09 |
| **Lacuna 1 fechada — reenvio de KYC não ganha estado novo.** Volta a `kyc_pendente`; o caso "reenviado" já existe no provedor como `status_reason = answered_waiting_analysis`. Contagem de tentativas vira atributo, não estado. `denied`/`fully_denied` não passa pelo contador — é final no provedor | [Pagar.me — objeto `kyc_details`](https://docs.pagar.me/page/wip-api-v4-adi%C3%A7%C3%A3o-do-fluxo-de-prova-de-vida), verificado nesta sessão: os 6 pares `status`/`status_reason` existem e `denied` está marcado "(Estado Final do KYC)" | 11/09 |
| **Lacuna 2 fechada — nenhum contrato expira por relógio, expira por resposta do provedor.** Antes de qualquer transição para `expirado_sem_pagamento`, consulta `GET /orders/{order_id}`. Estado novo `conciliacao_pendente` na cobrança (não no contrato). **Nenhum timeout foi escolhido** — seria número sem fonte | [Pagar.me — obter pedido](https://docs.pagar.me/reference/obter-pedido), verificado: GET `/orders/{order_id}` devolve `status`. Prazo de reenvio de webhook não é publicado → virou pergunta 20 da SPEC §4.6.2 | 11/09 |
| **Lacuna 3 fechada — não havia contradição, havia dois níveis.** Contrato e marco são máquinas diferentes; §1 já dava a hierarquia (marco é fonte da verdade, contrato é resumo calculado). Lista única em §10.0: 6 estados elegíveis, 6 não elegíveis, pré-condição "marco financiado", invariante `contrato.em_disputa ⟺ algum marco em disputa` | Hipótese levantada na orquestração e confirmada pelo `arquiteto-tecnico`; SPEC §12.2 conferida e não define lista concorrente | 11/09 |
| **4 transições que faltavam e 1 beco sem saída, achados na revisão adversarial** — marco `em_execucao`/`reenviado`/`agendado` → `em_disputa` (regras já escritas em §8.3/§8.4/§13.2 sem porta na máquina); marco `aguardando_pagamento` → `cancelado` (contrato expirado deixava o marco preso para sempre); `recusado_na_revisao` existia só em prosa | Inspeção automática de entrada/saída por estado nos 4 diagramas | 11/09 |
| **Inconsistência no trabalho do próprio especialista, corrigida antes de aplicar:** §10.0 declarava `em_permanencia`/`removido_antes_do_prazo` elegíveis a disputa sem as transições correspondentes no diagrama — a lista prometeria porta que a máquina não executa | Reinspeção de consistência após aplicar os blocos | 11/09 |
| **Disputa de permanência decide reputação, não dinheiro** — não é escolha entre três opções: cobrar o criador está fora do v1 (SPEC §4.3 camada 6, depende de advogado) e pagar pelo fundo mudaria o dimensionamento feito contra risco de cartão (SPEC §4.3.3). Sobra reputação, e só é honesta se a tela disser antes da contratação | Derivação de duas decisões já travadas; sujeita a veto do Marco | 11/09 |
| ✅ **FOUNDER DECISION (categoria C): disputa de permanência não move dinheiro no v1.** Direção D confirmada — registro de descumprimento, sem devolução, até existir mecanismo financeiro/antifraude/jurídico sustentável. Arquitetura preservada para reabrir com dado real | Decisão do Marco, 11/09, depois da análise NÃO COMPROVADA de B pelo `financeiro` | 11/09 |
| **Correção documental: SPEC §4.3 camada 5 corrigida de R$ 90/mês (comissão antiga 7,5%) para R$ 114/mês.** Não era conflito de decisão — a comissão de 9,5% já estava fechada em 3 lugares (CLAUDE.md, SPEC §4.4, SPEC §4.3.3). Única referência matematicamente obsoleta, corrigida sem levar ao Founder | Confirmado: nenhum outro cálculo dependia do valor R$ 90; R$ 1.800 de capital de giro (§14.2.3.2) é número coincidente e não relacionado | 11/09 |
| **Auditoria de terminologia: "disputa" cobria dois mecanismos diferentes.** "Direito de disputa" (única ocorrência, MÁQUINA §8.0.3) foi escrito para permanência na mesma máquina que trata disputa pré-liberação (que pode terminar em `executada`, dinheiro movido). Achado real: minha própria §10.0 (rodada anterior) tinha conflacionado os dois. Proposta de distinção registrada em `BLOCKERS.md`, **não aplicada aos documentos** — aguarda confirmação do Founder | Busca completa em SPEC/MÁQUINA/PRODUTO/FEATURE-MATRIX; precedente de §8.1 (mediação decide proporção só enquanto há escrow) | 11/09 |
| ✅ **Aplicado A/B/C (checkpoint d5166e7): permanência descumprida separada estruturalmente da máquina de disputa financeira.** Removidas as transições `em_permanencia/removido_antes_do_prazo → em_disputa`; movida a linha de permanência para a tabela de estados "não elegíveis" a disputa (mesma razão já usada para `concluido`/`cancelado`); adicionada `removido_antes_do_prazo → concluido`, só por correção do operador. Verificação formal de alcançabilidade: permanência nunca alcança `em_disputa`; os 5 estados financeiros continuam alcançando `em_disputa`/`executada`, intactos | Bug real encontrado na perícia: `concluido` do contrato = "todos os marcos aprovados", e marco chega a `em_permanencia` só depois de `aprovado` — logo o contrato já estava `concluido` quando `em_disputa` de permanência o reabria. Confirmado por grafo de alcançabilidade | 11/09 |
| **Critério objetivo para o operador distinguir remoção pelo criador de remoção pela rede: NÃO COMPROVADO, não inventado.** Registrado como pendência em MÁQUINA §10.0.1, não bloqueia a decisão fechada — só bloqueia escrever a tela dessa correção específica | Busca confirmou: a única menção anterior era texto meu próprio, não fonte independente; a API só detecta ausência do post, não o motivo | 11/09 |
| ✅ **Enforcement passa a ter camada que IMPEDE, não só avisa.** `guarda-comandos.py` roda em `PreToolUse` sobre Bash e devolve código 2 — bloqueio incondicional pela documentação oficial. Duas faixas: destrutiva sem escape, governança com escape explícito `APROVADO_PELO_FOUNDER=1` | Bloqueio comprovado ao vivo nesta sessão em ambas as faixas (`git merge --abort` e `git clean -fdn` recusados); escape comprovado liberando | 12/09 |
| **Os guardas deixam de depender da máquina do agente.** `verificar.py` roda as MESMAS funções do hook sobre o repositório inteiro, em um comando, com código de saída. Não há segunda cópia da regra para divergir — o verificador importa o guarda | PASS em 43 arquivos, saída 0; FAIL comprovado com violação deliberada em `FEATURE-MATRIX.md`, saída 1, revertido | 12/09 |
| **Suíte de regressão dos guardas: 37 testes, cada um protegendo uma falha REAL já ocorrida** — nenhum teste inventado. Cobre os 5 falsos positivos já consertados, as violações reais já encontradas, e o par bloqueado/liberado do guarda de comandos | `python3 .claude/guardas/testes.py` → PASS 37/37 | 12/09 |
| **Verificação independente no GitHub.** Fluxo `Guardas` roda testes + verificador em todo push e pull request. Um agente pode afirmar PASS; o repositório passa a verificar sozinho | `.github/workflows/guardas.yml`, 35 linhas, sem serviço externo e sem segredo | 12/09 |
| **Falso positivo real encontrado e consertado no mesmo turno: o guarda de comandos bloqueou a escrita da própria página que o documenta**, porque o texto CITAVA um comando destrutivo. Conserto de raiz: corpo de heredoc é dado escrito, não comando executado — nunca afrouxar a regra | Bloqueio observado ao vivo; 3 testes novos, incluindo os dois que provam que o conserto não abriu buraco (comando destrutivo antes e depois do heredoc continua bloqueado) | 12/09 |
| **Defeito real no escape: `APROVADO-PELO-FOUNDER=1` não é nome válido de variável de shell** — o terminal respondia "command not found". Renomeado para `APROVADO_PELO_FOUNDER=1` | Teste ao vivo: antes saída 127 (terminal recusou), depois saída 128 (guarda liberou, git recusou por não haver merge em andamento) | 12/09 |
| 🔴 **Proteção de branch NÃO existe e não foi possível criar neste ambiente.** API do GitHub responde `protected: false` para `main` e para o branch de trabalho. Registrado em `.claude/guardas/LEIA-ME.md` com o que configurar e onde — **não declarado como protegido** | `list_branches` em 12/09: ambos com `"protected": false` | 12/09 |
| **Nenhuma skill criada nesta rodada.** O único fluxo repetitivo comprovado — rodar os guardas antes do checkpoint — virou um comando e uma verificação automática. Skill duplicaria `CLAUDE.md` e o `LEIA-ME.md` sem acrescentar capacidade | Aplicação da pergunta obrigatória §2.2 ao próprio harness | 12/09 |


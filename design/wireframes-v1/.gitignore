# Arquivo publicável gerado pelo helper do skill "design" (payload do editor embutido, ~2,5 MB).
# Regenerável a qualquer momento a partir dos .dc.html + canvas.json — ver README.md.
influentz-wireframes-v1.html
